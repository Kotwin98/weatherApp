import React from 'react';

const Form = () => {
    return (
        <form>
            <input type="text" />
            <button>Search for a city</button>
        </form>
    );
};

export default Form;